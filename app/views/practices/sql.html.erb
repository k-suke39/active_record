<%= turbo_frame_tag "items" do %>
  <%= form_with url: '/practices' do |f|%>  
          <%= f.text_area :name, class: 'mt-1 rounded bg-color-primary text-white font-bold focus:outline-none focus:ring-0 focus:ring-color-primary focus:border-transparent', size: "55x20", value: "SELECT * FROM users"%>
  <% end%>
<% end%>